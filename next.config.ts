// import type { NextConfig } from "next";

const nextConfig = {
  /* config options here */
  output: "export",
  basePath: "/chatterbot-learnweb3",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
